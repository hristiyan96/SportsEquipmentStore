@model IEnumerable<SportsEquipmentStore.Models.Stock>

<h1>Stock</h1>
<a asp-action="Create" class="btn btn-primary">Add New Stock</a>

<table class="table">
    <thead>
        <tr>
            <th>Equipment</th>
            <th>Quantity</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Equipment.Name</td>
                <td>@item.Quantity</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-secondary btn-sm">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
